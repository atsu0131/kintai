.modal-dialog
  .modal-content
    %h1 勤務時間入力

    = form_with model: [@user, @timecard], remote: true, local: true do |f|
      %div
        = f.time_select :start_at, required: true
      %div
        = f.time_select :end_at, required: true
      %div= f.submit "作成"
  
